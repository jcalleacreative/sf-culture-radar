# Frontend

Simple static frontend for viewing top SF Culture Radar stories.

## Run locally

From repo root:

```bash
python3 -m http.server 4173 --directory frontend
```

Then open http://localhost:4173 and ensure the backend is running at http://localhost:8000.
